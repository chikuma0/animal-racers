# Initial revision2 contact-viewer snapshot

The compressed JavaScript is the exact bundle retained in the running loopback pose server while the18 September20 candidate contact views were saved. Its source hash map is copied in the manifest. The rules source predates the final finish-line pass-cue fix; pose rendering does not step those rules. This is not the later production build or the tail repair.

The current editable viewer subsequently added counter-reply timing and evade displacement. This snapshot retains the original contact-inspection implementation so those old captures are not silently attributed to changed tooling. All six original candidate GLBs remain under assets/source/western/revision2/candidate. No production backend or private configuration is included in this renderer-only bundle.
