# Initial pose-capture attempt — receipt synchronization failure

The canvas movies and18 UI screenshots were captured, but one saved-still receipt was reused because the harness accepted the previous Saved status before the next asynchronous save completed (17 unique receipts for18 stills). This run does not pass complete pose-evidence association. The repaired harness waits for a new Saved value and checks expected pose settings and loaded candidate hashes; a fresh run supplies acceptance evidence. The executed first harness and its manifest remain here.
