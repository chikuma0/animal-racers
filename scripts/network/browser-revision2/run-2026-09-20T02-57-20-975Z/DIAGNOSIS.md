# Retained pilot: not matrix acceptance

The frozen Lion-host/Wolf-guest native-WebRTC match completed with identical69.2/30.8 results and46/0HP. Both roles recorded an authoritative successful evade and damaging response. Observed-cue-to-completed-evade-click timing was412ms guest and392ms host. Public reports and screenshots are retained; no game state or clocks were patched.

The pilot failed its planned two-real-leaps-per-role oracle: host recorded one and guest two. The host's second requested tap coincided with the309m hurdle/stumble; the complete50-event race trace rules out history truncation. This does not establish a lost network input. Subsequent controller work moves the preceding steering pulse earlier and adds normal leap attempts in clear sections, keeping the original acceptance threshold.

This script version also stopped too soon after results to guarantee the full award reveal. Subsequent runs wait4.5seconds after both results screens. This recording is diagnostic and does not satisfy full-rematch/award acceptance.

The original null-muxer decode check reported duplicate DTS values at its default output timebase. Both entire raw videos subsequently decoded with no errors using passthrough timestamps and a microsecond encoder timebase. Both portable H264/960px/30fps/AAC outputs then decoded cleanly and retained their full duration. The original run's failure JSON remains unchanged; `media-recovery-*.json` and per-video metadata record the recovery, hashes and sizes. Only verified raw duplicates were removed.

The reply screenshot also shows an apparent separated tail/tuft, reported to the parent for visual review. These are same-machine functional observations under concurrent load, not internet, human, cinematic-art or physical-device acceptance.
