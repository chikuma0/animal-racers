# Exact-byte tail repair integration

All six approved GLBs and six editable Blender files were copied exactly into canonical paths. No source or animation was reauthored in this wave. `promotion.json` binds the public roster metadata, approved repair manifest, previous integration receipt and parent runtime review. `preserved.json` records 232 historical or unrelated files that this integration must not alter.

## Reproduction

Run `node scripts/assets/tail-integration.mjs reproduce`. The wrapper populates a disposable scratch checkout from the original twelve repair inputs and the historical freeze's other files. It verifies every frozen hash, copies the unchanged recipe and dependencies, and runs the original freeze check, fresh reconstruction, source inspection, actual GLB geometry, complete media decode and distorted-tail negative. Existing original receipts remain untouched; fresh receipts are saved here. Six runtime hashes and all editable mesh/topology/weights/colors/UV/morph/rig/action semantics must match.

`node scripts/assets/tail-integration.mjs verify` checks current canonical/source hashes, approved candidate equality and the preserved provenance chain. `verify-assets.mjs`, `verify-upright.mjs` and `verify-roster-v2.mjs --canonical` validate the actual promoted models and resolve the repair chain explicitly. The original `tail-repair/GATES.md` still describes its original freeze and is not relaxed to match today's changed production. Its commands are replayed in the reconstructed historical root.

## Four passes and precise compatibility limitation

1. Implemented exact promotion and separate provenance/reproduction wrappers. Original `canonical-integration.json`, recipe and prior candidate/evidence were preserved.
2. Reviewed the production inspectors: current hashes now resolve through the repair manifest; new runtime reports are written here. Upright evade checks both feet lift and land, retaining strict planted-foot checks for stationary actions and fixed limb lengths throughout.
3. The production floor oracle exposed an unused legacy quadruped `defeat` tail boundary. Measured whole-mesh minima are Lion −0.2162m, Wolf −0.1717m and Unicorn −0.5080m. The crossing vertices are exclusively tail-weighted; the non-tail body minimum is −0.00072m. All displayed clips retain their original floor bounds. No candidate byte or threshold was changed to hide a visible defect.
4. Parent authorized the precise compatibility scope after inspection. The race oracle retains finite/explosion bounds and tail-edge stretch below3.5× for this one unused clip, and keeps the original −0.20m limit on every body or mixed body/tail vertex. Every other race clip keeps the original whole-mesh limit; all upright clips retain their original −0.12m limit. A renderer-dispatch change fails the scope guard and requires renewed review.

Dispatch evidence: `src/championship/renderer.ts:1135` selects the upright form for fight/transition/results; line1175 selects defeat inside the fight branch; line1224 selects defeat for results. Countdown/race uses the race form. `floor-diagnostic.json` records actual exported vertex measurements; `diagnose-floor.mjs` reproduces them. This limitation does not approve visible floor penetration or certify unused quadruped combat as production artwork.

Current assets are frozen for root ordinary-control gameplay and delivery. No new motion render was necessary because promoted bytes equal the reviewed candidates. Full cinematic, physical-device and owner acceptance remain separate.
