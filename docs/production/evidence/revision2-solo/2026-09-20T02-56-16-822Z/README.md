Initial integrated solo controller run. Completed Lion/Wolf and any other retained report is valid functional evidence, but the harness captured results immediately before the3.6-second score reveal/cup ceremony completed. Stopped for capture-controller repair; subsequent final run waits4.5s at results. This interruption did not change game code or physics. Any incomplete recording is not complete-loop evidence.
The actual run ended on a controller DOM-read race: phase changed between reading race status and reading the removed progress label, causing a30s locator timeout. Final controller uses a bounded read and rechecks the visible phase. The separate cup capture wait was repaired at the same time.
