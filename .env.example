# Optional for solo; required for friend invitations. Public browser credentials only.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Set to the Git commit when creating a review build.
NEXT_PUBLIC_BUILD_REVISION=local-working-tree
